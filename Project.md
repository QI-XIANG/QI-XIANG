## 1. 晶圓廠自動化元件管理系統開發指南

針對開發系統的 IT 人員，若對 Fab 端（晶圓廠）知識及半導體專業術語不熟悉，可以透過以下幾個面向幫助其快速進入狀況，並理解專案的必要性與重要性：

### 一、 核心專業技術與名詞快速入門
IT 人員可以先將專案視為一個**「自動化資料庫整合與維護系統」**。以下是來源中提到的關鍵名詞解釋，以便 IT 人員快速理解業務邏輯：
* Pcell (Parameterized Cell)：在電路佈局設計中使用的「參數化元件」，是晶片設計的基本構件。
* Golden Pcell：指經過驗證（verified）且確認無誤的 Pcell
。建立此系統的目的就是要記錄這些「黃金標準」，以便未來專案開發時可以追溯使用。
* TDM (Technology Database Management)：世界先進內部的技術資料庫管理系統，本專案是在此架構下新增功能。
* SPICE Testline / Split Table：包含元件詳細資訊（如 Pcell 名稱、接腳 ID 等）的資料庫。系統需要透過這些表格進行資料對比與查詢。
* MPW & Lot ID：代表晶圓生產的批次編號，是追蹤元件來源的重要識別碼。
* DL2 (Device List)：系統內的元件清單，記錄了元件的性能與描述，是本系統的主要資料來源之一。

### 二、 為什麼需要執行這項專案？（專案重要性）

IT 人員理解「為何而戰」能更精準地設計系統邏輯：

* 建立可追溯性（Traceability）：過去可能缺乏統一記錄驗證過 Pcell 的流程，本專案旨在建立一個標準化流程，確保未來專案開發時有據可查。
* 自動化與效率提升：目前許多元件資訊（如 Pin ID、Pcell Name）需要從不同的 Split Table 中比對。透過此系統，可以從資料庫**自動 Mapping（映射）**出正確資訊，減少人工查表時間。
* 減少人為錯誤，提高精確度：系統在資料匯入（Import）與送審（Submit）時會進行多重檢查（如格式檢查、版本一致性檢查），確保 MPW、Lot ID 與接腳資訊的準確性。
* 確保版本一致性：系統會自動檢查 Golden Pcell 的版本是否與 TDM 系統版本一致，若不一致會發出警告，避免生產端使用到錯誤版本的資訊。

### 三、 IT 人員的開發重點與流程建議

對於不理解 Fab 端知識的 IT 人員，可以請其專注於以下 URD（使用者需求規格）中的技術開發項：
* 資料庫開發：建立 SPICE Split Table 的資料庫，並開發儲存與上傳程式。
* 使用者介面 (UI) 設計：在 TDM 入口新增一個名為「Device Golden Pcell」的頁面，功能包含選擇 TL4（技術代碼）、匯入元件清單、編輯、以及最重要的「取得 Pcell 接腳資訊 (Get Pcell Pin Information)」按鈕。
* 自動化映射邏輯：撰寫 SQL 查詢，根據使用者填寫的 SPICE Testline 和 Block，自動從資料庫抓取對應的 Pcell Name 和 Pin ID。
* 異常預防機制：開發檢查程式，例如當元件清單與正式生效的版本有差異時，系統需發出郵件通知相關負責人。

### 四、 如何更快進入狀況？

* 參考 URD 中的流程圖：來源中提供了詳細的 Working Flow，從選擇 TL4 到存儲（Save/Submit）的每一步驟都有說明，IT 人員應以此為開發藍圖。
* 與 TD、DS 及 PDK 團隊協作：TD 是文件的擁有者，而 DS、SPICE 和 PDK 團隊會協助確認 Pcell 與資料庫路徑，IT 人員在遇到專業名詞不確定時，應直接詢問這幾個單位的需求。
* 從「資料流」切入：IT 人員不一定要懂半導體物理，但要懂資料怎麼跑（從 DL2 產出清單 -> Excel 編輯 -> 匯入系統 -> Mapping 資料庫 -> 取得 Pin 資訊並存檔）。
* 補充說明：以上資訊直接節錄自您的來源文件。若您需要，我可以為這份開發提案產生測驗（Quiz）重點報告（Tailored Report），幫助 IT 人員快速檢測自己對系統邏輯的理解程度。

---

## 2. Device Golden Pcell 管理系統開發手冊

這份報告旨在協助 IT 開發人員快速掌握「Device Golden Pcell 管理系統」的專案核心。本專案的主要目標是建立一套標準化流程，記錄並追蹤經過驗證（Verified）的 Golden Pcell，以便未來專案開發時能有據可查並提升自動化效率。

### 一、 為什麼要執行這項專案？（專案重要性與效益）

* 建立可追溯性 (Traceability)：過去缺乏統一記錄驗證過 Pcell 的系統，本專案旨在確保每個元件的來源（如 MPW、Lot ID）與驗證狀態都能被精確記錄。
* 自動化取代人工比對：目前需手動比對元件資訊，新系統可透過資料庫映射（Mapping），自動抓取正確的 Pcell 名稱與接腳 (Pin ID) 資訊，大幅減少人為錯誤。
* 確保生產一致性：系統會自動比對 Golden Pcell 版本與 TDM 系統版本是否一致，若有差異會發出告警，避免生產端使用錯誤資訊。

### 二、 IT 人員必須掌握的專業名詞

IT 開發時可將這些名詞視為資料表欄位或過濾條件：
* Pcell / Golden Pcell：參數化元件。所謂「Golden」是指已經過驗證且確認無誤的標準版本。
* TDM (Technology Database Management)：本專案所屬的母系統（技術資料庫管理系統）。
* DL2 (Device Performance)：系統的主要資料來源，提供元件清單 (Device List)。
* SPICE Testline / Split Table：存放元件詳細規格（如 Pcell 名稱、接腳 ID）的原始資料表，IT 需將其整合進資料庫供系統查詢。
* MPW & Lot ID：晶圓生產批次編號，用於識別元件的生產來源。

### 三、 系統開發重點 (URD 核心功能)

IT 人員的開發任務主要集中在以下四大模組：

* 資料庫維護模組 (Database Development)
* 開發 SPICE Split Table 的儲存與上傳程式，將 Excel/CSV 格式的原始資料標準化存入資料庫。實作格式檢查機制，確保匯入的資料符合規格。
* 使用者介面模組 (UI/UX)
* 在 TDM 入口新增「Device Golden Pcell」頁面。
* 功能按鈕需包含：Select TL4 (選擇技術代碼)、Export/Import Excel、Edit 以及核心功能 Get Pcell Pin Information。
* 自動映射邏輯 (Auto Mapping Logic)，這是系統的核心：根據使用者填寫的 SPICE Testline 與 Block，透過 SQL 查詢 DS 資料庫，自動帶回對應的 Pcell Name、Pin Name 及 Pin ID。
* 實作自動填入邏輯，減少使用者手動輸入的負擔。
* 異常預警與檢核機制，版本檢查：當 DCN 送審時，系統須比對 TDM 版本與 Golden Pcell 版本是否一致，不一致時須阻斷送審並通知負責人。
* 資料一致性警報：每日自動檢查 DL2 元件清單與 Golden Pcell 的差異，若有異動（新增或刪除）需發送 Email 通知權責單位進版。

### 四、 開發時程規劃

* 系統開發完成：預計 2026 年 6 月底。
* 使用者測試 (UAT)：預計 2026 年 8 月底。
* 正式上線 (Go-live)：預計 2026 年 9 月初。

給 IT 人員的建議： 開發時請務必參考 URD 中的 Working Flow (作業流程圖)。雖然 Fab 端的物理知識較深奧，但對 IT 而言，這是一個典型的「多來源資料整合與檢核系統」。重點在於確保從 DL2 抓取的清單與 DS 資料庫中的 Split Table 能透過邏輯正確串接並維持版本同步。

---

## 3. 系統異常預警與自動通知機制指南

根據提供的來源文件，系統的異常預警與通知機制主要透過「每日電子郵件告警」與「系統介面警告訊息」兩種方式來通知相關人員：

### 1. 每日自動電子郵件告警 (Alarm Email)

系統會自動進行每日檢查，確保資料的一致性：
* 通知觸發條件：系統每日會自動檢查「正式生效的 DL2 (Device List)」與「Golden Pcell」內容是否一致。
* 通知內容：若兩者不一致（例如 DL2 有新增或刪除元件），系統會發送 Email 通知權責單位進行 Golden Pcell 的進版操作。
* 通知對象：包含 MB Owner、Golden Pcell 修改人員及其主管。

### 2. 系統介面即時警告訊息 (System Warning Messages)

在使用者操作過程中，系統會針對不同情境彈出訊息或警告：

* 元件清單異動提醒：當使用者選擇 TL4 匯入清單時，若發現元件在 DL2 已被新增或刪除，系統會發出警告訊息。若元件在 DL2 已不存在，系統會在畫面上將該元件內容反灰並鎖住編輯功能，標示該元件將於 Submit 後刪除。
* 送審版次不一致 (DCN Submission)：當進行 DCN 送審時，系統會檢查「TDM 版本」與「Golden Pcell 版本」是否一致。
* 通知內容：若版本不一致，系統會發出訊息請 Owner 將 Golden Pcell 提交至與 TDM 相同的版本，否則將無法通過檢查。
* 資料格式與邏輯錯誤：手動/匯入錯誤：在「Submit」階段，程式會檢查欄位格式。若備註（Remark）選擇特定選項但相關欄位不是空白，系統會跳出訊息要求修改。
* 命名不符：若 Pcell Name 與 Device Name 不一致，系統會彈出訊息提示確認。
* 資料庫轉換異常：在產生 Pin ID 的過程中，若程式偵測到格式錯誤（如 T/L # 重複、缺少 Pin assignment 欄位等），會列出異常清單供開發或維護人員參考。

### 3. 流程退回機制 (Gating Flow)

在 SPICE TL Split Table 產生過程中，若 DS 進行格式檢查發現異常，會將文件退回給 TD 修正，這也是一種變相的異常通知機制。

---

## 4. Pcell 版本一致性稽核與同步機制

根據來源文件，系統自動比對 Pcell 版本一致性的機制主要分為每日自動稽核、送審關卡比對以及元件資訊細節比對三個層面：

### 1. 每日自動稽核 (Daily Automatic Audit)

系統會定期執行自動檢查，確保資料庫間的同步：
* 稽核頻率：系統每天會自動執行檢查。
* 比對對象：比對「正式生效的 DL2 (Device List)」與「Golden Pcell」的內容是否一致。
* 異常處理：若發現兩者不一致（例如 DL2 有新增或刪除元件），系統會自動發送 Email 告警給 MB Owner、Golden Pcell 修改人員及其主管，通知相關權責單位進行進版操作。

### 2. DCN 送審時的版次比對 (Gating Check)

在文件正式送審階段，系統設有強制性的版本校驗關卡：
* 觸發條件：當 DCN 送審版本為 1.0 以上，且 Device List 是由 TDM 送審時。
* 比對項目：系統會檢查「TDM 版本」是否與「Golden Pcell 版本」完全一致。
* 強制執行：若版本不一致，系統會發出訊息阻斷流程，要求 Owner 必須將 Golden Pcell 提交至與 TDM 預送審版相同的版次後，才可繼續送審。

### 3. 元件資訊細節比對 (Field-Level Comparison)

當使用者在系統介面操作（如選擇 TL4 帶入資料）時，系統會進行即時比對：
比對基準：系統會比對以下三個欄位，只要其中一個有差異即判定為不一致：

* Device Type (元件類型)
* Device Description (元件描述)
* Device Name (元件名稱)
* 差異顯示：
    * 新增元件：若 DL2 有新元件而 Golden Pcell 尚未記錄，系統會自動新增該筆資料。
    * 刪除元件：若元件在 DL2 已不存在，系統會在 Golden Pcell 介面上將該元件內容反灰並鎖住編輯功能，並標示該元件將於 Submit 後自動刪除。
    * 此外，系統在進行 Import Excel 操作時，也會自動確認 Excel 內的元件順序與內容是否與系統表格一致，若比對不同則會停止匯入，以確保資料的精確性。

---

## 1. 什麼是 DCN？

DCN 在此專案中指的是正式的文件/設計變更送審機制（Document/Design Change Notice）。其在系統中的關鍵作用如下：

* 版本校驗關卡：當 DCN 送審的版本在 1.0 以上，且 Device List 是由 TDM 系統送審時，系統會強制執行「版次比對」。
* 一致性檢查：系統會檢查「TDM 版本」是否與「Golden Pcell 版本」完全一致。
* 阻斷機制：若比對結果不一致，系統會發出訊息要求負責人將 Golden Pcell 提交至預送審的版次，否則無法通過 DCN 送審流程。

## 2. 什麼是 MB？

來源文件中提到的 MB 主要出現在 MB Owner 這個角色上，其代表模型庫（Model Bank）或元件模型的權責人員：

* 異常通知對象：當系統每日自動檢查發現「正式生效的 DL2 (Device List)」與「Golden Pcell」內容不一致時（例如有元件新增或刪除），系統會發送告警 Email 給 MB Owner、Golden Pcell 修改人員及其主管。
* 進版權責單位：MB Owner 是接收通知並負責執行 Golden Pcell 進版（Version update）操作的關鍵人員，以確保模型資料與 Device List 同步。
* 總結來說，DCN 是一個正式的送審流程關卡，確保資料發佈前經過檢核；而 MB Owner 則是負責維護元件模型一致性的權責人員。

## 3. 什麼是「進版」?

在該系統中是指將 Golden Pcell 的版本進行更新或提交（Submit），以確保其資料內容與版本與其他系統（如 DL2 或 TDM）保持一致的動作。具體包含以下幾個層面的意涵：

* 資料一致性的同步：系統每天會自動檢查「正式生效的 DL2 (Device List)」與「Golden Pcell」是否有差異（例如元件的新增或刪除）。若發現不一致，系統會發送 Email 告警，要求權責單位進行「進版」操作，將 Golden Pcell 更新至最新狀態。
* 版本號碼的對齊：在進行 DCN 送審流程時，如果送審版本在 1.0 以上，系統會強制檢查 「TDM 版本」與「Golden Pcell 版本」是否一致。若兩者不符，負責人（Owner）必須將 Golden Pcell 重新執行「Submit」動作，將其版次提升至與 TDM 預送審版相同的版本，否則流程會被阻斷。
* 必要的作業程序：文件中特別提到，即便 DL2 的元件基本資訊（Type, Description, Name）沒有變動，若 TDM 系統版次有變，仍需到 Golden Pcell 頁面執行一次 Submit 動作來完成「進版」，以符合審核規範。
* 權責單位： 「進版」動作通常由 MB Owner、Golden Pcell 修改人員及其主管負責執行。
